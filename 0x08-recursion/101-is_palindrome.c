#include "main.h"

/**
 * is_palindrome - checks if a string is a palindrome or not
 * @s: string to check
 * Return: 1 or 0
 */

int is_palindrome(char *s)
{
	if (*s == " ")
		return (0);
	return (check_palindrome(f, n));
}
