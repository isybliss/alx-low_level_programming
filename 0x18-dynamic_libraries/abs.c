#include "main.h"
/**
 * _abs - function that computes the absolute value of and integer.
 * @n: integer to get absolute value from
 * Return: 0
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	return (n);
}

