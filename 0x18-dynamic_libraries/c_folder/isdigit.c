#include "main.h"

/**
 * _isdigit - function that checks for a digit
 * @c: character to represent digit
 * Return: 1 (success), 0 (failure)
 */

int _isdigit(int c)
{
	return (c >= 48 && c <= 57);
}

